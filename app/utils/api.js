import { get, post, postJson, uploadFile, resource } from '@/repositories/appApi.js'

export { get, post, postJson, uploadFile, resource }

export default { get, post, postJson, uploadFile, resource }
